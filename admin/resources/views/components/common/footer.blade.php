
  </div>
  </div>
          <footer>
       <div class="bottom-0 right-0 fixed w-full border-t h-15 bg-white dark:bg-gray-900 dark:border-gray-600 ">
                    <div
                        class="flex items-center justify-center ml-52 py-4 text-xs text-gray-600 dark:text-gray-200">
                        <p>© 2025 ihookwebsolutions. All rights reserved.</p>
                    </div>
                </div>
            </footer>
</main>
 </div>
        <!-- ===== Content Area End ===== -->
</div>


<!-- dropdown  -->
    <!-- networks dropdown    -->
    <script>
        const networksToggle = document.getElementById("networks-toggle");
        const networksMenu = document.getElementById("networks-menu");
        const networksIcon = document.getElementById("networks-icon");

        networksToggle.addEventListener("click", () => {
            networksMenu.classList.toggle("hidden");

            if (networksMenu.classList.contains("hidden")) {
                // arrow
                networksIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                networksIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!-- Team dropdown  -->
    <script>
        const teamToggle = document.getElementById("team-toggle");
        const teamMenu = document.getElementById("team-menu");
        const teamIcon = document.getElementById("team-icon");

        teamToggle.addEventListener("click", () => {
            teamMenu.classList.toggle("hidden");

            if (teamMenu.classList.contains("hidden")) {
                // arrow
                teamIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                teamIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!-- estore dropdown    -->
    <script>
        const estoreToggle = document.getElementById("estore-toggle");
        const estoreMenu = document.getElementById("estore-menu");
        const estoreIcon = document.getElementById("estore-icon");

        estoreToggle.addEventListener("click", () => {
            estoreMenu.classList.toggle("hidden");

            if (estoreMenu.classList.contains("hidden")) {
                // arrow
                estoreIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                estoreIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!-- party dropdown    -->
    <script>
        const partyToggle = document.getElementById("party-toggle");
        const partyMenu = document.getElementById("party-menu");
        const partyIcon = document.getElementById("party-icon");

        partyToggle.addEventListener("click", () => {
            partyMenu.classList.toggle("hidden");

            if (partyMenu.classList.contains("hidden")) {
                // arrow
                partyIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                partyIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!--compansation dropdown    -->
    <script>
        const compansationToggle = document.getElementById("compansation-toggle");
        const compansationMenu = document.getElementById("compansation-menu");
        const compansationIcon = document.getElementById("compansation-icon");


        compansationToggle.addEventListener("click", () => {
            compansationMenu.classList.toggle("hidden");

            if (compansationMenu.classList.contains("hidden")) {
                // arrow
                compansationIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                compansationIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!--marketing dropdown    -->
    <script>
        const marketingToggle = document.getElementById("marketing-toggle");
        const marketingMenu = document.getElementById("marketing-menu");
        const marketingIcon = document.getElementById("marketing-icon");


        marketingToggle.addEventListener("click", () => {
            marketingMenu.classList.toggle("hidden");

            if (marketingMenu.classList.contains("hidden")) {
                // arrow
                marketingIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                marketingIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!--ticket dropdown    -->
    <script>
        const ticketToggle = document.getElementById("ticket-toggle");
        const ticketMenu = document.getElementById("ticket-menu");
        const ticketIcon = document.getElementById("ticket-icon");


        ticketToggle.addEventListener("click", () => {
            ticketMenu.classList.toggle("hidden");

            if (ticketMenu.classList.contains("hidden")) {
                // arrow
                ticketIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                ticketIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!--signup dropdown    -->
    <script>
        const signupToggle = document.getElementById("signup-toggle");
        const signupMenu = document.getElementById("signup-menu");
        const signupIcon = document.getElementById("signup-icon");


        signupToggle.addEventListener("click", () => {
            signupMenu.classList.toggle("hidden");

            if (signupMenu.classList.contains("hidden")) {
                // arrow
                signupIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                signupIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!--E-wallet dropdown    -->
    <script>
        const ewalletToggle = document.getElementById("ewallet-toggle");
        const ewalletMenu = document.getElementById("ewallet-menu");
        const ewalletIcon = document.getElementById("ewallet-icon");


        ewalletToggle.addEventListener("click", () => {
            ewalletMenu.classList.toggle("hidden");

            if (ewalletMenu.classList.contains("hidden")) {
                // arrow
                ewalletIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                ewalletIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!--E-pin dropdown    -->
    <script>
        const epinToggle = document.getElementById("epin-toggle");
        const epinMenu = document.getElementById("epin-menu");
        const epinIcon = document.getElementById("epin-icon");


        epinToggle.addEventListener("click", () => {
            epinMenu.classList.toggle("hidden");

            if (epinMenu.classList.contains("hidden")) {
                // arrow
                epinIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                epinIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!-- packages dropdown    -->
    <script>
        const packagesToggle = document.getElementById("packages-toggle");
        const packagesMenu = document.getElementById("packages-menu");
        const packagesIcon = document.getElementById("packages-icon");


        packagesToggle.addEventListener("click", () => {
            packagesMenu.classList.toggle("hidden");

            if (packagesMenu.classList.contains("hidden")) {
                // arrow
                packagesIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                packagesIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!--shopping cart dropdown    -->
    <script>
        const shoppingToggle = document.getElementById("shopping-toggle");
        const shoppingMenu = document.getElementById("shopping-menu");
        const shoppingIcon = document.getElementById("shopping-icon");


        shoppingToggle.addEventListener("click", () => {
            shoppingMenu.classList.toggle("hidden");

            if (shoppingMenu.classList.contains("hidden")) {
                // arrow
                shoppingIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                shoppingIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!--report dropdown    -->
    <script>
        const reportToggle = document.getElementById("report-toggle");
        const reportMenu = document.getElementById("report-menu");
        const reportIcon = document.getElementById("report-icon");


        reportToggle.addEventListener("click", () => {
            reportMenu.classList.toggle("hidden");

            if (reportMenu.classList.contains("hidden")) {
                // arrow
                reportIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                reportIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!-- repot-user dropdown    -->
    <script>
        const userToggle = document.getElementById("user-toggle");
        const userMenu = document.getElementById("user-menu");
        const userIcon = document.getElementById("user-icon");


        userToggle.addEventListener("click", () => {
            userMenu.classList.toggle("hidden");

            if (userMenu.classList.contains("hidden")) {
                // arrow
                userIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                userIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!-- AD-Campaign dropdown    -->
    <script>
        const adcampaignToggle = document.getElementById("adcampaign-toggle");
        const adcampaignMenu = document.getElementById("adcampaign-menu");
        const adcampaignIcon = document.getElementById("adcampaign-icon");


        adcampaignToggle.addEventListener("click", () => {
            adcampaignMenu.classList.toggle("hidden");

            if (adcampaignMenu.classList.contains("hidden")) {
                // arrow
                adcampaignIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                adcampaignIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!-- Settings dropdown    -->
    <script>
        const settingsToggle = document.getElementById("settings-toggle");
        const settingsMenu = document.getElementById("settings-menu");
        const settingsIcon = document.getElementById("settings-icon");


        settingsToggle.addEventListener("click", () => {
            settingsMenu.classList.toggle("hidden");

            if (settingsMenu.classList.contains("hidden")) {
                // arrow
                settingsIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                settingsIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!-- sales dropdown    -->
    <script>
        const salesToggle = document.getElementById("sales-toggle");
        const salesMenu = document.getElementById("sales-menu");
        const salesIcon = document.getElementById("sales-icon");


        salesToggle.addEventListener("click", () => {
            salesMenu.classList.toggle("hidden");

            if (salesMenu.classList.contains("hidden")) {
                // arrow
                salesIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                salesIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!-- system dropdown    -->
    <script>
        const systemToggle = document.getElementById("system-toggle");
        const systemMenu = document.getElementById("system-menu");
        const systemIcon = document.getElementById("system-icon");


        systemToggle.addEventListener("click", () => {
            systemMenu.classList.toggle("hidden");

            if (systemMenu.classList.contains("hidden")) {
                // arrow
                systemIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                systemIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!-- Personalization  dropdown    -->
    <script>
        const personalizationToggle = document.getElementById("personalization-toggle");
        const personalizationMenu = document.getElementById("personalization-menu");
        const personalizationIcon = document.getElementById("personalization-icon");


        personalizationToggle.addEventListener("click", () => {
            personalizationMenu.classList.toggle("hidden");

            if (personalizationMenu.classList.contains("hidden")) {
                // arrow
                personalizationIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                personalizationIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!-- payment dropdown    -->
    <script>
        const paymentToggle = document.getElementById("payment-toggle");
        const paymentMenu = document.getElementById("payment-menu");
        const paymentIcon = document.getElementById("payment-icon");


        paymentToggle.addEventListener("click", () => {
            paymentMenu.classList.toggle("hidden");

            if (paymentMenu.classList.contains("hidden")) {
                // arrow
                paymentIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                paymentIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!-- cms dropdown    -->
    <script>
        const cmsToggle = document.getElementById("cms-toggle");
        const cmsMenu = document.getElementById("cms-menu");
        const cmsIcon = document.getElementById("cms-icon");


        cmsToggle.addEventListener("click", () => {
            cmsMenu.classList.toggle("hidden");

            if (cmsMenu.classList.contains("hidden")) {
                // arrow
                cmsIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                cmsIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!-- notification dropdown    -->
    <script>
        const notificationToggle = document.getElementById("notification-toggle");
        const notificationMenu = document.getElementById("notification-menu");
        const notificationIcon = document.getElementById("notification-icon");


        notificationToggle.addEventListener("click", () => {
            notificationMenu.classList.toggle("hidden");

            if (notificationMenu.classList.contains("hidden")) {
                // arrow
                notificationIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                notificationIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!-- cart dropdown    -->
    <script>
        const cartToggle = document.getElementById("cart-toggle");
        const cartMenu = document.getElementById("cart-menu");
        const cartIcon = document.getElementById("cart-icon");


        cartToggle.addEventListener("click", () => {
            cartMenu.classList.toggle("hidden");

            if (cartMenu.classList.contains("hidden")) {
                // arrow
                cartIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                cartIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!-- logs dropdown    -->
    <script>
        const logsToggle = document.getElementById("logs-toggle");
        const logsMenu = document.getElementById("logs-menu");
        const logsIcon = document.getElementById("logs-icon");


        logsToggle.addEventListener("click", () => {
            logsMenu.classList.toggle("hidden");

            if (logsMenu.classList.contains("hidden")) {
                // arrow
                logsIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                logsIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!-- sponsor dropdown    -->
    <script>
        const sponsorToggle = document.getElementById("sponsor-toggle");
        const sponsorMenu = document.getElementById("sponsor-menu");
        const sponsorIcon = document.getElementById("sponsor-icon");


        sponsorToggle.addEventListener("click", () => {
            sponsorMenu.classList.toggle("hidden");

            if (sponsorMenu.classList.contains("hidden")) {
                // arrow
                sponsorIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                sponsorIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!-- history dropdown    -->
    <script>
        const historyToggle = document.getElementById("history-toggle");
        const historyMenu = document.getElementById("history-menu");
        const historyIcon = document.getElementById("history-icon");


        historyToggle.addEventListener("click", () => {
            historyMenu.classList.toggle("hidden");

            if (historyMenu.classList.contains("hidden")) {
                // arrow
                historyIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                historyIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!-- premium dropdown    -->
    <script>
        const premiumToggle = document.getElementById("premium-toggle");
        const premiumMenu = document.getElementById("premium-menu");
        const premiumIcon = document.getElementById("premium-icon");


        premiumToggle.addEventListener("click", () => {
            premiumMenu.classList.toggle("hidden");

            if (premiumMenu.classList.contains("hidden")) {
                // arrow
                premiumIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                premiumIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!-- coaching dropdown    -->
    <script>
        const coachingToggle = document.getElementById("coaching-toggle");
        const coachingMenu = document.getElementById("coaching-menu");
        const coachingIcon = document.getElementById("coaching-icon");


        coachingToggle.addEventListener("click", () => {
            coachingMenu.classList.toggle("hidden");

            if (coachingMenu.classList.contains("hidden")) {
                // arrow
                coachingIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                coachingIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!-- commission dropdown    -->
    <script>
        const commissionToggle = document.getElementById("commission-toggle");
        const commissionMenu = document.getElementById("commission-menu");
        const commissionIcon = document.getElementById("commission-icon");


        commissionToggle.addEventListener("click", () => {
            commissionMenu.classList.toggle("hidden");

            if (commissionMenu.classList.contains("hidden")) {
                // arrow
                commissionIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                commissionIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!-- pv dropdown    -->
    <script>
        const pvToggle = document.getElementById("pv-toggle");
        const pvMenu = document.getElementById("pv-menu");
        const pvIcon = document.getElementById("pv-icon");


        pvToggle.addEventListener("click", () => {
            pvMenu.classList.toggle("hidden");

            if (pvMenu.classList.contains("hidden")) {
                // arrow
                pvIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // down arrow
                pvIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <!--distributor-table-script-->
    <script>
        if (document.getElementById("default-table") && typeof simpleDatatables.DataTable !== 'undefined') {
            const dataTable = new simpleDatatables.DataTable("#default-table", {
                searchable: false,
                perPageSelect: false
            });
        }
    </script>

    <!--Timer-script:starts-->
    <script>
        function startTime() {
            var today = new Date();
            var hr = today.getHours();
            var min = today.getMinutes();
            var sec = today.getSeconds();
            ap = (hr < 12) ? "<span>AM</span>" : "<span>PM</span>";
            hr = (hr == 0) ? 12 : hr;
            hr = (hr > 12) ? hr - 12 : hr;
            //Add a zero in front of numbers<10
            hr = checkTime(hr);
            min = checkTime(min);
            sec = checkTime(sec);
            document.getElementById("clock").innerHTML = hr + ":" + min + ":" + sec + " " + ap;

            var months = ['January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December'];
            var days = ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'];
            var curWeekDay = days[today.getDay()];
            var curDay = today.getDate();
            var curMonth = months[today.getMonth()];
            var curYear = today.getFullYear();
            var date = curWeekDay + ", " + curDay + " " + curMonth + " " + curYear;
            document.getElementById("date").innerHTML = date;

            var time = setTimeout(function () { startTime() }, 500);
        }
        function checkTime(i) {
            if (i < 10) {
                i = "0" + i;
            }
            return i;
        }
        startTime();
    </script>

    <!-- Chart.js CDN (for Graph) -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <script>
        const ctx = document.getElementById('myChart');
        new Chart(ctx, {
            type: 'line',
            data: {
                labels: ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun'],
                datasets: [{
                    label: 'Performance',
                    data: [65, 59, 80, 81, 56, 70],
                    borderColor: 'rgb(89, 98, 112)',
                    backgroundColor: 'rgb(229, 231, 235)',
                    tension: 0.3,
                    fill: true,
                }]
            }
        });
    </script>

    <!-- responsive script  -->
    <script>
        const toggleBtnx = document.getElementById('asidexToggle');
        const asidex = document.getElementById('asidex');

        toggleBtnx.addEventListener('click', () => {
            asidex.classList.toggle('-translate-x-full');
        });
    </script>

    <!-- dark and light theme -->
    <script>
        const toggle = document.getElementById('themeToggle');

        // Check and apply stored theme
        if (localStorage.theme === 'dark') {
            document.documentElement.classList.add('dark');
            toggle.checked = true;
        } else {
            document.documentElement.classList.remove('dark');
            toggle.checked = false;
        }

        // Toggle listener
        toggle.addEventListener('change', function () {
            if (this.checked) {
                document.documentElement.classList.add('dark');
                localStorage.theme = 'dark';
            } else {
                document.documentElement.classList.remove('dark');
                localStorage.theme = 'light';
            }
        });
    </script>

    <script src="{{ asset('js/bundle.js') }}"></script>



<!-- Masters toggle script -->
    <script>
        const mastersToggle = document.getElementById("masters-toggle");
        const mastersMenu = document.getElementById("masters-menu");
        const mastersIcon = document.getElementById("masters-icon");

        mastersToggle.addEventListener("click", () => {
            mastersMenu.classList.toggle("hidden");

            if (mastersMenu.classList.contains("hidden")) {
                // plus icon
                mastersIcon.innerHTML =`<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m10 16 4-4-4-4"/>`;
            } else {
                // minus icon
                mastersIcon.innerHTML = `<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 10 4 4 4-4"/>`;
            }
        });
    </script>

    <script>

        if (document.getElementById("search-table") && typeof simpleDatatables.DataTable !== 'undefined') {
            const dataTable = new simpleDatatables.DataTable("#search-table", {
                searchable: true,
                sortable: false
            });
        }

    </script>
    <!--distributor-table-script-->

    <!-- data table script  -->

    <script>

        if (document.getElementById("filter-table") && typeof simpleDatatables.DataTable !== 'undefined') {
            const dataTable = new simpleDatatables.DataTable("#filter-table", {
                tableRender: (_data, table, type) => {
                    if (type === "print") {
                        return table
                    }
                    const tHead = table.childNodes[0]
                    const filterHeaders = {
                        nodeName: "TR",
                        attributes: {
                            class: "search-filtering-row"
                        },
                        childNodes: tHead.childNodes[0].childNodes.map(
                            (_th, index) => ({
                                nodeName: "TH",
                                childNodes: [
                                    {
                                        nodeName: "INPUT",
                                        attributes: {
                                            class: "datatable-input",
                                            type: "search",
                                            "data-columns": "[" + index + "]"
                                        }
                                    }
                                ]
                            })
                        )
                    }
                    tHead.childNodes.push(filterHeaders)
                    return table
                }
            });
        }

    </script>


    <!-- export table  -->
    <script>

        if (document.getElementById("export-table") && typeof simpleDatatables.DataTable !== 'undefined') {

            const exportCustomCSV = function (dataTable, userOptions = {}) {
                // A modified CSV export that includes a row of minuses at the start and end.
                const clonedUserOptions = {
                    ...userOptions
                }
                clonedUserOptions.download = false
                const csv = simpleDatatables.exportCSV(dataTable, clonedUserOptions)
                // If CSV didn't work, exit.
                if (!csv) {
                    return false
                }
                const defaults = {
                    download: true,
                    lineDelimiter: "\n",
                    columnDelimiter: ";"
                }
                const options = {
                    ...defaults,
                    ...clonedUserOptions
                }
                const separatorRow = Array(dataTable.data.headings.filter((_heading, index) => !dataTable.columns.settings[index]?.hidden).length)
                    .fill("+")
                    .join("+"); // Use "+" as the delimiter

                const str = separatorRow + options.lineDelimiter + csv + options.lineDelimiter + separatorRow;

                if (userOptions.download) {
                    // Create a link to trigger the download
                    const link = document.createElement("a");
                    link.href = encodeURI("data:text/csv;charset=utf-8," + str);
                    link.download = (options.filename || "datatable_export") + ".txt";
                    // Append the link
                    document.body.appendChild(link);
                    // Trigger the download
                    link.click();
                    // Remove the link
                    document.body.removeChild(link);
                }

                return str
            }

            const table = new simpleDatatables.DataTable("#export-table", {
                template: (options, dom) => "<div class='" + options.classes.top + "'>" +
                    "<div class='flex flex-col sm:flex-row sm:items-center space-y-4 sm:space-y-0 sm:space-x-3 rtl:space-x-reverse w-full sm:w-auto'>" +
                    (options.paging && options.perPageSelect ?
                        "<div class='text-xs text-gray-600 dark:text-gray-300 " + options.classes.dropdown + "'>" +
                    "<label>" +
                    "<select class='text-xs p-2 " + options.classes.selector + "'></select> " + options.labels.perPage +
                    "</label>" +
                    "</div>" : ""
                ) + "<button id='exportDropdownButton' type='button' class='flex w-28 items-center justify-center rounded-lg border border-gray-300 bg-white px-4 py-2 text-xs font-medium text-gray-800 hover:bg-gray-100 hover:text-gray-900 focus:z-10 dark:border-gray-700 dark:bg-gray-900 dark:text-gray-300 dark:hover:bg-gray-800 dark:hover:text-white'>" +
                "Export as" +
                "<svg class='-me-0.5 ms-1.5 h-3.5 w-3.5' aria-hidden='true' width='24' height='24' fill='none' viewBox='0 0 24 24'>" +
                "<path stroke='currentColor' stroke-linecap='round' stroke-linejoin='round' stroke-width='2' d='m19 9-7 7-7-7' />" +
                "</svg>" +
                "</button>" +
                "<div id='exportDropdown' class='z-10 overflow-hidden hidden w-auto rounded-lg bg-white shadow dark:bg-gray-800' data-popper-placement='bottom'>" +
                "<ul class=' text-left text-xs text-gray-600 dark:text-gray-300 divide-y divide-gray-200 dark:divide-gray-700' aria-labelledby='exportDropdownButton'>" +
                "<li class=''>" +
                "<button id='export-csv' class='group inline-flex w-full items-center px-4 py-2 text-xs text-gray-500 hover:bg-gray-100 hover:text-gray-900 dark:text-gray-400 dark:hover:bg-gray-600 dark:hover:text-white'>" +
                "<svg class='me-1.5 h-4 w-4 text-gray-400 group-hover:text-gray-900 dark:text-gray-400 dark:group-hover:text-white' aria-hidden='true' xmlns='http://www.w3.org/2000/svg' width='24' height='24' fill='currentColor' viewBox='0 0 24 24'>" +
                "<path fill-rule='evenodd' d='M9 2.221V7H4.221a2 2 0 0 1 .365-.5L8.5 2.586A2 2 0 0 1 9 2.22ZM11 2v5a2 2 0 0 1-2 2H4a2 2 0 0 0-2 2v7a2 2 0 0 0 2 2 2 2 0 0 0 2 2h12a2 2 0 0 0 2-2 2 2 0 0 0 2-2v-7a2 2 0 0 0-2-2V4a2 2 0 0 0-2-2h-7Zm1.018 8.828a2.34 2.34 0 0 0-2.373 2.13v.008a2.32 2.32 0 0 0 2.06 2.497l.535.059a.993.993 0 0 0 .136.006.272.272 0 0 1 .263.367l-.008.02a.377.377 0 0 1-.018.044.49.49 0 0 1-.078.02 1.689 1.689 0 0 1-.297.021h-1.13a1 1 0 1 0 0 2h1.13c.417 0 .892-.05 1.324-.279.47-.248.78-.648.953-1.134a2.272 2.272 0 0 0-2.115-3.06l-.478-.052a.32.32 0 0 1-.285-.341.34.34 0 0 1 .344-.306l.94.02a1 1 0 1 0 .043-2l-.943-.02h-.003Zm7.933 1.482a1 1 0 1 0-1.902-.62l-.57 1.747-.522-1.726a1 1 0 0 0-1.914.578l1.443 4.773a1 1 0 0 0 1.908.021l1.557-4.773Zm-13.762.88a.647.647 0 0 1 .458-.19h1.018a1 1 0 1 0 0-2H6.647A2.647 2.647 0 0 0 4 13.647v1.706A2.647 2.647 0 0 0 6.647 18h1.018a1 1 0 1 0 0-2H6.647A.647.647 0 0 1 6 15.353v-1.706c0-.172.068-.336.19-.457Z' clip-rule='evenodd'/>" +
                "</svg>" +
                "<span>Export CSV</span>" +
                "</button>" +
                "</li>" +
                "<li>" +
                "<button id='export-json' class='group inline-flex w-full items-center px-4 py-2 text-xs text-gray-500 hover:bg-gray-100 hover:text-gray-900 dark:text-gray-400 dark:hover:bg-gray-600 dark:hover:text-white'>" +
                "<svg class='me-1.5 h-4 w-4 text-gray-400 group-hover:text-gray-900 dark:text-gray-400 dark:group-hover:text-white' aria-hidden='true' xmlns='http://www.w3.org/2000/svg' width='24' height='24' fill='currentColor' viewBox='0 0 24 24'>" +
                "<path fill-rule='evenodd' d='M9 2.221V7H4.221a2 2 0 0 1 .365-.5L8.5 2.586A2 2 0 0 1 9 2.22ZM11 2v5a2 2 0 0 1-2 2H4v11a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V4a2 2 0 0 0-2-2h-7Zm-.293 9.293a1 1 0 0 1 0 1.414L9.414 14l1.293 1.293a1 1 0 0 1-1.414 1.414l-2-2a1 1 0 0 1 0-1.414l2-2a1 1 0 0 1 1.414 0Zm2.586 1.414a1 1 0 0 1 1.414-1.414l2 2a1 1 0 0 1 0 1.414l-2 2a1 1 0 0 1-1.414-1.414L14.586 14l-1.293-1.293Z' clip-rule='evenodd'/>" +
                "</svg>" +
                "<span>Export JSON</span>" +
                "</button>" +
                "</li>" +
                "<li>" +
                "<button id='export-txt' class='group inline-flex w-full items-center px-4 py-2 text-xs text-gray-500 hover:bg-gray-100 hover:text-gray-900 dark:text-gray-400 dark:hover:bg-gray-600 dark:hover:text-white'>" +
                "<svg class='me-1.5 h-4 w-4 text-gray-400 group-hover:text-gray-900 dark:text-gray-400 dark:group-hover:text-white' aria-hidden='true' xmlns='http://www.w3.org/2000/svg' width='24' height='24' fill='currentColor' viewBox='0 0 24 24'>" +
                "<path fill-rule='evenodd' d='M9 2.221V7H4.221a2 2 0 0 1 .365-.5L8.5 2.586A2 2 0 0 1 9 2.22ZM11 2v5a2 2 0 0 1-2 2H4v11a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V4a2 2 0 0 0-2-2h-7ZM8 16a1 1 0 0 1 1-1h6a1 1 0 1 1 0 2H9a1 1 0 0 1-1-1Zm1-5a1 1 0 1 0 0 2h6a1 1 0 1 0 0-2H9Z' clip-rule='evenodd'/>" +
                "</svg>" +
                "<span>Export TXT</span>" +
                "</button>" +
                "</li>" +
                "<li>" +
                "<button id='export-sql' class='group inline-flex w-full items-center px-4 py-2 text-xs text-gray-500 hover:bg-gray-100 hover:text-gray-900 dark:text-gray-400 dark:hover:bg-gray-600 dark:hover:text-white'>" +
                "<svg class='me-1.5 h-4 w-4 text-gray-400 group-hover:text-gray-900 dark:text-gray-400 dark:group-hover:text-white' aria-hidden='true' xmlns='http://www.w3.org/2000/svg' width='24' height='24' fill='currentColor' viewBox='0 0 24 24'>" +
                "<path d='M12 7.205c4.418 0 8-1.165 8-2.602C20 3.165 16.418 2 12 2S4 3.165 4 4.603c0 1.437 3.582 2.602 8 2.602ZM12 22c4.963 0 8-1.686 8-2.603v-4.404c-.052.032-.112.06-.165.09a7.75 7.75 0 0 1-.745.387c-.193.088-.394.173-.6.253-.063.024-.124.05-.189.073a18.934 18.934 0 0 1-6.3.998c-2.135.027-4.26-.31-6.3-.998-.065-.024-.126-.05-.189-.073a10.143 10.143 0 0 1-.852-.373 7.75 7.75 0 0 1-.493-.267c-.053-.03-.113-.058-.165-.09v4.404C4 20.315 7.037 22 12 22Zm7.09-13.928a9.91 9.91 0 0 1-.6.253c-.063.025-.124.05-.189.074a18.935 18.935 0 0 1-6.3.998c-2.135.027-4.26-.31-6.3-.998-.065-.024-.126-.05-.189-.074a10.163 10.163 0 0 1-.852-.372 7.816 7.816 0 0 1-.493-.268c-.055-.03-.115-.058-.167-.09V12c0 .917 3.037 2.603 8 2.603s8-1.686 8-2.603V7.596c-.052.031-.112.059-.165.09a7.816 7.816 0 0 1-.745.386Z'/>" +
                "</svg>" +
                "<span>Export SQL</span>" +
                "</button>" +
                "</li>" +
                "</ul>" +
                "</div>" + "</div>" +
                (options.searchable ?
                    "<div class='text-xs " + options.classes.search + "'>" +
                    "<input class='text-xs " + options.classes.input + "' placeholder='" + options.labels.placeholder + "' type='search' title='" + options.labels.searchTitle + "'" + (dom.id ? " aria-controls='" + dom.id + "'" : "") + ">" +
                    "</div>" : ""
                ) +
                "</div>" +
                "<div class='text-xs " + options.classes.container + "'" + (options.scrollY.length ? " style='height: " + options.scrollY + "; overflow-Y: auto;'" : "") + "></div>" +
                "<div class='text-xs " + options.classes.bottom + "'>" +
                (options.paging ?
                    "<div class='text-xs " + options.classes.info + "'></div>" : ""
                ) +
                "<nav class='text-xs " + options.classes.pagination + "'></nav>" +
                "</div>"
            })
            const $exportButton = document.getElementById("exportDropdownButton");
            const $exportDropdownEl = document.getElementById("exportDropdown");
            const dropdown = new Dropdown($exportDropdownEl, $exportButton);
            console.log(dropdown)

            document.getElementById("export-csv").addEventListener("click", () => {
                simpleDatatables.exportCSV(table, {
                    download: true,
                    lineDelimiter: "\n",
                    columnDelimiter: ";"
                })
            })
            document.getElementById("export-sql").addEventListener("click", () => {
                simpleDatatables.exportSQL(table, {
                    download: true,
                    tableName: "export_table"
                })
            })
            document.getElementById("export-txt").addEventListener("click", () => {
                simpleDatatables.exportTXT(table, {
                    download: true
                })
            })
            document.getElementById("export-json").addEventListener("click", () => {
                simpleDatatables.exportJSON(table, {
                    download: true,
                    space: 3
                })
            })
        }

    </script>

 </body>
</html>
